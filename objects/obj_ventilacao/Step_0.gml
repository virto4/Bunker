/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
depth = -y
if cliques == total_fita and fita {
	if current_time > tempo_alpha + 20 {
		alpha -= 0.05
		tempo_alpha = current_time
	} else {
		instance_destroy()
	}
}